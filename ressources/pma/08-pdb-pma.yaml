apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: phpmyadmin-pdb
  namespace: projet-pma
  labels:
    app: phpmyadmin
    tier: frontend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: phpmyadmin
